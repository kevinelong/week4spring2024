function getNumGrandkids() {
    // in a better example, we might look this up in a database
    return 6;
}
let num = getNumGrandkids();//changed case on Kids to kids
console.log(num);

console.log("# grandkids is " + getNumGrandkids());//changed case on Kids to kids


function add(num1, num2) {
    let sum = num1 + num2;
    return sum;
}

// elsewhere
let a = 10, b = 20, c;
c = add(a, b);
console.log("The sum of " + a + " and " + b + " is " + c);